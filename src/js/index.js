// Javascript Entry Point //

import React from 'react';
import ReactDOM from 'react-dom';
import App from './app';

ReactDOM.render(
  <App className='dex' container='octo'/>,
  document.querySelector(".app")
);
